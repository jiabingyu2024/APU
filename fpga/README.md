# Archived FPGA Attempt

This directory keeps the early PL-only/PYNQ-Z2 attempt for reference. It is not the final project route and should not be used for current acceptance testing.

Current final PYNQ route is the DMA branch:

- Code: `dma/`
- Overlay: `dma/overlay/apu_dma.bit` and `dma/overlay/apu_dma.hwh`
- Documents: `docs/dma/`

Related archived documents are in `docs/archive/fpga/`.
