# Archived SoC Attempt

This directory keeps the PicoRV32 + APU SoC attempt for reference. It is not the final project route and should not be used for current acceptance testing.

Current final route:

- Base APU RTL: `rtl/`
- PYNQ MMIO baseline: `apuYjb/`
- Final DMA branch: `dma/`

Related archived documents are in `docs/archive/soc/`.
