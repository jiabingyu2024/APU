# Third Party Code

This directory is kept for historical SoC exploration dependencies. The current final DMA route does not require running or modifying these files.

`third_party/picorv32/` belongs to the archived PicoRV32 + APU SoC attempt. See `docs/archive/soc/` and `docs/archive/explore/` for the related documents.
